[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run railway:setup"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
