[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'node railway-setup.js && node server.js'"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
