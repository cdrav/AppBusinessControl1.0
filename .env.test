# Variables de entorno para testing
NODE_ENV=test
JWT_SECRET=test_secret_key_for_testing_only
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=business_control_test
DB_PORT=3306

# Email configuration (mock para testing)
EMAIL_USER=test@example.com
EMAIL_PASS=test_password
